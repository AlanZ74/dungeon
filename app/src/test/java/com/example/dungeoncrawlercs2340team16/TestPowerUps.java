package com.example.dungeoncrawlercs2340team16;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

import android.widget.ImageView;

import org.junit.Test;
public class TestPowerUps {
    private Player p = Player.getPlayer("placeholder", "placeholder_char");
//    @Test
//    public void testCollision() {
//        CharInfo player = new CharInfo(new ImageView(null), 100, 200);
//        PowerUpInfo powerUp = new PowerUpInfo(new ImageView(null), 50, 30); // Adjust these values based on your PowerUpInfo class
//        boolean notCollided = true;
//
//        boolean result = p.checkPowerUpCollide(notCollided, player, powerUp);
//
//        assertFalse(result); // Expecting collision
//    }
//
//    @Test
//    public void testNoCollision() {
//        CharInfo player = new CharInfo(new ImageView(null), 100, 200);
//        PowerUpInfo powerUp = new PowerUpInfo(new ImageView(null), 300, 30); // Adjust these values based on your PowerUpInfo class
//        boolean notCollided = true;
//
//        boolean result = p.checkPowerUpCollide(notCollided, player, powerUp);
//
//        assertTrue(result); // Expecting no collision
//    }
//
//    @Test
//    public void testAlreadyCollided() {
//        CharInfo player = new CharInfo(new ImageView(null), 100, 200);
//        PowerUpInfo powerUp = new PowerUpInfo(new ImageView(null), 50, 30); // Adjust these values based on your PowerUpInfo class
//        boolean notCollided = false;
//
//        boolean result = p.checkPowerUpCollide(notCollided, player, powerUp);
//
//        assertFalse(result); // Expecting false because it's already collided
//    }
}
